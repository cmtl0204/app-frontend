services:
    app-frontend:
        build:
            context: ..
            dockerfile: devops/Dockerfile
        container_name: frontend-qa
        restart: unless-stopped
        networks:
            - npm_network

networks:
    npm_network:
        external: true
